spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:33061/mysql
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5Dialect
    database: mysql
    show-sql: true